version: '2.0'

# services:
  # jenkins: 
#     image: 'jenkins/jenkins:lts'
#      ports:
#       - '8081:8080'
#     volumes:
#       - jenkins-home:/var/jenkins_home
# version: '2.0'

services:
  petStoreBackend:
    build: pet-services/
    ports:
      - "9999:9999"
    image: pet-backend

  petStoreFront:
    build: storefront/
    ports:
      - "5000:80"
    image: pet-front